<%= render "layouts/header" %>

    <% @roasts.each do |roast| %>
    <%= link_to roast do %>
<div class="col-sm-6 col-md-4">
  <div class="thumbnail thumb-box">
    <h2 style="text-align:center;">
      <%= roast.body %>
    </h2>

    <%= image_tag(roast.image, size: "250x300" ) %>

    <div class="caption" style="text-align:center;">

    </div>
  </div>
</div>
<% end %>
<% end %>
